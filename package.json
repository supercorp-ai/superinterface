{
  "name": "@superinterface/superinterface",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run build -- --watch"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "turbo": "^1.11.3"
  }
}
