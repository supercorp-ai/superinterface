{
  "name": "@superinterface/superinterface",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --force",
    "dev": "turbo run build --force -- --watch"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@swc/core": "^1.4.2",
    "turbo": "^1.13.0"
  }
}
